{
  "name": "genyo-modernist-timer",
  "private": true,
  "version": "2.0.0",
  "description": "Mostra tempo restante do expediente no modernist",
  "scripts": {
    "dev": "node scripts/generate-icons.mjs && node scripts/build-extension.mjs --watch",
    "build": "node scripts/generate-icons.mjs && node scripts/build-extension.mjs",
    "icons": "node scripts/generate-icons.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
